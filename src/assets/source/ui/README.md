# UI source assets

- `home_assist_glow-master.png`: high-resolution generated working source.
- `home_assist_glow.png`: 360 x 360 transparent export consumed by the LVGL image converter.

Keep dynamic text, numbers, status icons, and hit targets out of these images. They belong in the shared LVGL UI layer.

